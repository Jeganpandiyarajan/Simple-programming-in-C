#include<stdio.h>
#include<conio.h>
int main(){
    int n,fact=1,i;
    printf("Enter the value of n :");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        fact=fact*i;
    }
    printf("\n%d",fact);
    getchar();
    return 0;
}
